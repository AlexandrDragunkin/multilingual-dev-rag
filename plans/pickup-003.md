# Pickup — plan 003 (для новой сессии)

> Компактная точка подхвата. Полная летопись — в `handoff-003.md`.
> Эта сессия закрыта из-за контекста; новая начинает отсюда.

## Где мы сейчас (одной фразой)

План 003 (автоуборка процессов-дубликатов `dev_rag.mcp_server`) **полностью
готов к релизу**: 87 unit-тестов (+1 регрессия бага 5), **получен
доказательный `guard.log` с `killed=1`** на живых процессах (см. 6c-3).
Ветка `feat/process-guard-003` готова к пушу по команде автора.

## Что нового в этой сессии (важно!)

- **Найден и исправлен баг 5** в `select_victims`: при старте инстанса под
  одним клиентом защита убивала дубли в ЧУЖОМ клиенте (нарушение multi-client
  plan 001). Не ловился прежними unit-тестами — там чужой клиент всегда был
  одиночкой. Выявлено dry-run на живой системе. Коммит `767ff65`.
- **Тесты:** `pytest tests/` → **87 passed** (16→17 в `test_process_guard`).
- **Коммитов в ветке:** 10 (было 9). Подробности — в обновлённом `handoff-003.md`
  (разделы 2b/4/5/6b/11).

## Факты, которые надо знать сразу

- **Репо:** `C:\REPO\multilingual-dev-rag`, ветка `feat/process-guard-003`
  (от `main` @ `b472078`), **не запушена**.
- **Тесты:** `pytest tests/` → **87 passed** (70 базовых + 16 plan 003 + 1 регрессия бага 5).
- **Python:** `C:/venv310-64/Scripts/python.exe` (3.10.8, единственный с zvec).
- **10 коммитов** в ветке, conventional (`build/feat/fix/test/docs/chore`).
- **Номер 003 — не rename!** «plan 002» уже занят будущим планом по zvec 0.6.0.

## Что делает plan 003

При старте нового `dev_rag.mcp_server` автоматически убирает висящие копии
(дубликаты от переподключений MCP-клиентов). Стратегия **newest-wins по
общему GUI-предку**:
- два real-инстанса в одной группе, если пересекаются их множества живых
  предков (кроме корневых explorer/systemd/...);
- в группе выживает самый свежий, остальные убиваются;
- сироты (все предки мертвы) — убиваются;
- разные GUI-клиенты (VSCode vs ZCode) друг друга не трогают (multi-client
  plan 001 сохранён).

Файлы: `src/dev_rag/process_guard.py`, `src/dev_rag/mcp_server.py` (хук в
`main`), `tests/test_process_guard.py` (16 кейсов), `pyproject.toml`
(`psutil>=5.9`).

## Что уже проверено

- **17 unit-тестов** — покрывают 5 найденных багов + базовые сценарии.
- **Dry-run на живой системе** (2026-07-21, две итерации):
  - ДО фикса бага 5: ZCode (свежий) убивал бы 2 чужих VSCode-инстанса.
  - ПОСЛЕ фикса: ZCode → 0 жертв; VSCode-дубли будут убраны только свежим
    VSCode-инстансом при его старте (что корректно).

## Что осталось (опционально)

**Все цели приёмки выполнены, и сверх того.** Получено два независимых
доказательства работы `process_guard` на живых процессах:

1. **Синтетический тест** (10:38, 10:41) — handoff 6c-3.
2. **Естественное срабатывание от VSCode** (10:54) — handoff 6c-4: при
   запуске VSCode и открытии RAG-сессии защита трижды сработала за 90с
   в живом окружении, убирая дубли от переподключений. Финальный
   `scripts/guard.log` (6 строк):

```
2026-07-21 08:14:51 my_pid=40556 marked=11 real_candidates=5 killed=0 victims=[none]
2026-07-21 10:38:51 my_pid=40672 marked=4  real_candidates=2 killed=1 victims=[pid=24004]
2026-07-21 10:41:42 my_pid=21216 marked=4  real_candidates=2 killed=1 victims=[pid=5220]
2026-07-21 10:53:24 my_pid=50116 marked=2  real_candidates=1 killed=0 victims=[none]
2026-07-21 10:54:13 my_pid=22064 marked=9  real_candidates=4 killed=1 victims=[pid=13204, pid=17632, pid=19952]
2026-07-21 10:54:55 my_pid=8512  marked=4  real_candidates=2 killed=1 victims=[pid=22064]
```

Строки 4-6 — **естественная работа в живом VSCode-сценарии**, без моков и
без синтетики. Подтверждает, что проблема накопления дублей существует
сегодня и `process_guard` её решает.

**Нерешённое (внешнее, вне scope):**
- ZCode не прокидывает `DEV_RAG_PROCESS_GUARD_LOG` из `.mcp.json` в env.
  Это не ломает защиту (она работает тихо), но мешает наблюдать её в
  ZCode-driven сценарии. Возможно стоит зарепортить в трекер ZCode.

## Архитектурное решение (принято исполнителем, не подтверждено автором)

Граница клиента = общий GUI-предок. Два consumer'а RAG под одним VSCode
(Claude extension + забытый task) схлопываются как дубль. Обоснование в
`handoff-003.md` раздел 2c. **Если автор не согласен — это нужно менять.**

## Что НЕ делать

- **Не путать с plan 002** (zvec 0.6.0 tokenizer).
- **Не пушить без явной команды автора.**
- **Не трогать `k3-mcp-server`** (отдельный репо `C:\REPO\ARLINE\k3-mcp-server`).
- **Не переименовывать ветку.**

## Команды для быстрого старта

```bash
cd C:/REPO/multilingual-dev-rag
git branch --show-current          # feat/process-guard-003
git log --oneline main..HEAD      # 10 коммитов
C:/venv310-64/Scripts/python.exe -m pytest tests/    # 87 passed
C:/venv310-64/Scripts/python.exe -m pytest tests/test_process_guard.py -v
```

## Что разумно сделать дальше (на усмотрение автора)

1. **Пуш + PR + тег `v0.2.0`** → всё готово (тесты, end-to-end, docs).
2. **Зарепортить в трекер ZCode** баг с прокидыванием env-переменной
   `DEV_RAG_PROCESS_GUARD_LOG` (не блокирует релиз, но улучшит
   наблюдаемость в будущем).
3. **Пересмотреть решение 2c** → если автор хочет различать Claude extension
   и VSCode task под одним VSCode.
